*** GIT Users: Read This ***

After cloning LaTIM-SNAP from Git, you need to execute the following commands
to initialize the submodules. LaTIM-SNAP will not compile without submodules

    git submodule init
    git submodule update

Alternatively, pass the '--recursive' option to git clone 
